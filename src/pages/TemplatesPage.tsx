import React, { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { LayoutTemplate } from 'lucide-react'
import { getTemplateCatalog } from '../api/knowledge'
import type { TemplateCatalog } from '../types/knowledge'
export default function TemplatesPage() { const [templates, setTemplates] = useState<TemplateCatalog | null>(null); useEffect(() => { void getTemplateCatalog().then(setTemplates).catch(console.error) }, []); if (!templates) return <div className="p-8 text-slate-400">Loading templates…</div>; return <div className="mx-auto max-w-5xl space-y-6"><div><h1 className="flex items-center gap-2 text-3xl font-extrabold text-white"><LayoutTemplate className="text-brand-400"/> Article templates</h1><p className="mt-1 text-slate-400">Start every document with a structure that works for people and AI.</p></div><div className="grid gap-4 md:grid-cols-2">{Object.entries(templates).map(([key, template]) => <div key={key} className="rounded-xl border border-slate-800 bg-slate-900/30 p-5"><div className="text-xs font-bold uppercase tracking-wider text-brand-400">{key}</div><h2 className="mt-2 font-bold text-white">{template.name}</h2><p className="mt-1 text-sm text-slate-400">{template.description}</p><div className="mt-4 flex flex-wrap gap-2">{template.sections.map(section => <span key={section} className="rounded-full bg-slate-950 px-2.5 py-1 text-[11px] text-slate-400">{section}</span>)}</div><Link to={`/articles/new?type=${key}`} className="mt-5 inline-block text-xs font-semibold text-brand-400">Use template →</Link></div>)}</div></div> }
